import styled from 'styled-components';

export const ProviderScheduleWrapper = styled('div')`
  .middle-section {
    width: 100%;
    display: flex;
    place-content: center;
    align-items: center;
    gap: 10px;
    padding: 10px;
  }
  .rbc-toolbar {
    display: none;
  }
  .rbc-time-slot {
    min-height: 20px;
    border-bottom: 1px solid grey;
    border-left: 0;
  }

  .rbc-time-slot {
    min-height: 20px;
    border-bottom: 1px solid grey;
    border-left: 0;
  }
`;
