import { Row } from 'antd';
import React, { FC } from 'react';
import { EmpStatusWrapper } from './EmpStatus.styled';

export const EmpStatus: FC = () => {
  return (
    <EmpStatusWrapper>
    </EmpStatusWrapper>
  );
};
