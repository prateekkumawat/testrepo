import { Row } from "antd";
import { styled } from "styled-components";

export const ReferringProvidersHeader = styled(Row) `
  display: flex;
  justify-content: space-between;
  width: 100%;
  .provider-filters {
    width: 70%;
    display: flex;
    justify-content: space-between;
  }
`
