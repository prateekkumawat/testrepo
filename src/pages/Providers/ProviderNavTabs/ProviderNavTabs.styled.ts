import { styled } from "styled-components";

export const ProviderTabWrapper = styled.div `
  .btn-group {
    min-width: 40%;
    display: flex;
    justify-content: flex-end;
    button {
      min-width: 25% !important;
      height: 50px;
      display: grid;
      place-content: center;
    }
  }
`
